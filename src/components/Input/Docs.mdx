import { Meta, Story, Primary, Canvas, Controls } from '@storybook/blocks'
import {Input} from './Input'
import * as InputStories from './Input.stories'

<Meta of={InputStories} component={Input}/>

# Input
This is an input molecule, used for capturing and submitting user data.

### Component
<Primary />
{/* ### Controls
<Controls /> */}


## Examples
<Canvas additionalActions={[
    {
      title: 'Open in GitHub',
      onClick: () => {
        window.open(
          'https://github.com/Tonybsilva-dev/Draupnir/blob/master/src/components/Input/Input.tsx',
          '_blank'
        );
      },
    }
  ]}
  withToolbar>
  <div className={"grid grid-cols-2 gap-2"}>
   <div>
    <h3 className="text-lg font-semibold mb-2">Default Input With Prefix - Enabled</h3>
      <Story of={InputStories.Primary} />   
    <h3 className="text-lg font-semibold mt-4 mb-2">Default Input - Not Enabled</h3>
      <Story of={InputStories.Disabled} />
    </div>

    <div>
      <h3 className="text-lg font-semibold mb-2">Input With Error Message</h3>
        <Story of={InputStories.WithMessageError} />
      </div>
  </div>
</Canvas>


## Usage
<div>

```jsx dark
import * as Input from 'your-design-system';

// Example with Zod
        <Controller
          name="search"
          control={control}
          render={({ field }) => (
            <Input.Root errorMessage={errors.search?.message}>
              <Input.Prefix>
                <Search className="w-5 h-5" />
              </Input.Prefix>
              <Input.Control
                {...field}
                placeholder="Search..."
                aria-label="Search field"
                autoComplete="off"
              />
            </Input.Root>
          )}
        />
```

</div>

